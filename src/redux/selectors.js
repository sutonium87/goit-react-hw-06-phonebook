// Selector function to get the contacts from the state
export const getContacts = state => state.contacts.contacts;

// Selector function to get the filter value from the state
export const getFilter = state => state.filter.filter;
